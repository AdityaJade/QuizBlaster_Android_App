<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.support.v4.app.quizblaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExamPageA" />
        <activity android:name=".ExamPageB" />
        <activity android:name=".ExamPageC" />
        <activity android:name=".ExamPageD" />
        <activity android:name=".English" />
        <activity android:name=".GKnowledge" />
        <activity android:name=".LReasoning" />
        <activity android:name=".QAptitude" />
        <activity android:name=".MainActivity">
            <!--This app is created by Aditya J -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>